/**
 * HTTPS public HTTP API origin. Same meaning as SPA `VITE_PUBLIC_API_BASE_URL`.
 * Must match `manifest.json` `host_permissions` for the target environment.
 * Prod value matches RiffSyncApi-prod `HttpApiUrl` / baked SPA env on riffsync.tv.
 */
export const PUBLIC_API_BASE_URL = 'https://hu8u08y3a5.execute-api.us-east-1.amazonaws.com'
