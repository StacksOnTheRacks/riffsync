# cfn-lint when run from `infra/cdk` (see `.github/workflows/ci.yml`).
# W3005: CDK often emits DependsOn that duplicate implicit ordering via GetAtt — noisy, not actionable here.
ignore_checks:
  - W3005
